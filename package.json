{
  "name": "dehu-unificado",
  "version": "0.2.0-beta.1",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node build.mjs",
    "package": "npm run build && zip -r dehu-unificado.zip manifest.json dist icons LICENSE README.md",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "~5.6.0",
    "vitest": "^2.0.0",
    "@types/chrome": "^0.0.268"
  }
}
